require 'rubygems'
require 'bundler'
require "bundler/gem_tasks"
require 'rake/testtask'
require 'rake/clean'

task :default => [:test]

desc 'Run tests (default)'
Rake::TestTask.new(:test) do |t|
  # todo: rspec
end
